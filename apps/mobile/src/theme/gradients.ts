// Zoop brand gradient colors - single source of truth
export const gradients = {
  primary: {
    start: "#9C2CF3",
    end: "#3A6FF9",
  },
};

// Convenience exports for common use cases
export const PRIMARY_GRADIENT: [string, string] = [
  gradients.primary.start,
  gradients.primary.end,
];
